https://console.cloud.google.com/home/dashboard?project=robq-terraform-demo

https://cloud.google.com/sdk/docs/quickstart-windows

https://cloud.google.com/kubernetes-engine/?hl=en_GB&_ga=2.11284660.-1691547645.1537275112



# Commands in Powershell or CMD

gcloud info

gcloud init

gloud projects list

gcloud auth list

# to authenticate your gcloud use :
gcloud auth application-default login

# To set the active account, run:
$ gcloud config set account `ACCOUNT`

Your project default Compute Engine zone has been set to [us-central1-a].
You can change it by running [gcloud config set compute/zone NAME].

Your project default Compute Engine region has been set to [us-central1].
You can change it by running [gcloud config set compute/region NAME].

Created a default .boto configuration file at [C:\Users\Rob\.boto]. See this file and
[https://cloud.google.com/storage/docs/gsutil/commands/config] for more
information about configuring Google Cloud Storage.
Your Google Cloud SDK is configured and ready to use!

* Commands that require authentication will use robq71@gmail.com by default
* Commands will reference project `robq-terraform-demo` by default
* Compute Engine commands will use region `us-central1` by default
* Compute Engine commands will use zone `us-central1-a` by default

Run `gcloud help config` to learn how to change individual settings

This gcloud configuration is called [default]. You can create additional configurations if you work with multiple accounts and/or projects.
Run `gcloud topic configurations` to learn more.

Some things to try next:

* Run `gcloud --help` to see the Cloud Platform services you can interact with. And run `gcloud help COMMAND` to get help on any gcloud command.
* Run `gcloud topic -h` to learn about advanced features of the SDK like arg files and output formatting

gcloud components install kubectl

# kubes commands

kubectl get pods

kubectl get nodes

# NAME                                                  STATUS    ROLES     AGE       VERSION
// gke-robq-terraform-d-robq-terraform-d-49248f6a-mqss   Ready     <none>    12m       v1.10.7-gke.1
// gke-robq-terraform-d-robq-terraform-d-b681f26e-zd0h   Ready     <none>    19m       v1.10.7-gke.1
// gke-robq-terraform-d-robq-terraform-d-c9057768-gnzw   Ready     <none>    16m       v1.10.7-gke.1

# 5e_First_Kube_app - Builds the plugin required for the implementation of the Application...

https://github.com/sl1pm4t/terraform-provider-kubernetes

mkdir -p /c/Go/src/github.com/sl1pm4t; cd /c/Go/src/github.com/sl1pm4t
git clone https://github.com/sl1pm4t/terraform-provider-kubernetes
cd /c/Go/src/github.com/sl1pm4t/terraform-provider-kubernetes

https://superuser.com/questions/701141/how-to-add-more-commands-to-git-bash

Go to ezwinports.
Download make-4.1-2-without-guile-w32-bin.zip (get the version without guile).
Extract zip.
Copy the contents to your Git\mingw64\ merging the folders, but do NOT overwrite/replace any existing files.

make fmt
make build

terraform init -plugin-dir=$GOPATH/bin



